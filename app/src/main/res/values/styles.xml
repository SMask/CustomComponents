<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- TextView -->
    <style name="TextView_Base">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:ellipsize">end</item>
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/main_font</item>
        <item name="android:textColorHint">@color/main_hint</item>
        <item name="android:textSize">@dimen/main_font</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- EditText -->
    <style name="EditText_Base" parent="TextView_Base">
        <item name="android:cursorVisible">true</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <!-- 删除ViewEditText -->
    <style name="CleanEditText_Base">
        <item name="textColor">@color/main_font</item>
        <item name="textColorHint">@color/main_hint</item>
        <item name="clean_res">@drawable/icon_delete</item>
        <item name="textSize">@dimen/main_font</item>
    </style>

    <!-- IndicatorView -->
    <style name="IndicatorView_Base">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="indicator_margin">6dp</item>
        <item name="indicator_colorNormal">@color/white</item>
        <item name="indicator_colorSelected">@color/white</item>
        <item name="indicator_style">RECT</item>
        <item name="indicator_radiusNormal">16dp</item>
        <item name="indicator_radiusSelected">30dp</item>
        <item name="indicator_widthNormal">16dp</item>
        <item name="indicator_widthSelected">30dp</item>
        <item name="indicator_heightNormal">4dp</item>
        <item name="indicator_heightSelected">4dp</item>
        <item name="indicator_corners">4dp</item>
    </style>

</resources>
